import { Given, When, Then } from "../../fixtures/fixture";

Then("I ensure that all the remaining balance is not captured", async ({ contactLensesProductPage }) => {
  //await contactLensesProductPage.clickOnAddToCart();
});
