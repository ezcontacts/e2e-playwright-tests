{
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.1",
    "cross-env": "^10.1.0",
    "playwright-bdd": "^8.4.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "scripts": {
    "auth": "node saveGoogleSession.js",
    "pretest": "npx playwright-bdd",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "debug": "npx playwright test --grep @debug",
    "test:validation": "cross-env ENV=prod playwright test --grep @validation",
    "test:homePage": "cross-env ENV=prods npx playwright test --grep @homePage_UI"
  }
}
