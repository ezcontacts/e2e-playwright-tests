import { Given, test, Then, When } from "../../fixtures/fixture";

When('the user clicks on the {string} option from the My Account menu', async () => {
 
});

Then('the Wish List page should be displayed', async () => {
  
});

Then('the page URL should contain {string}', async () => {

});

Then('the page should display the heading as {string}', async () => {
  
});


